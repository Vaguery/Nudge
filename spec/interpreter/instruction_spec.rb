require File.join(File.dirname(__FILE__), "/../spec_helper")

describe "Instruction" do
  
  it "should have a name parameter, with no default"
  
  it "should have a symbol as a name"
  
  it "should have a requirements attribute, which defaults to an empty hash"
  
  it "should have an effects attribute, which defaults to an empty hash"
end