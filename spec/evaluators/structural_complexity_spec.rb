require File.join(File.dirname(__FILE__), "/../spec_helper")
include Nudge

describe "number of points" do
  it "should add its score to a given individual's scores hash"
  
  # and how exactly would you propose doing that?
  # is the Evaluator the actor, the Individual, or something that jams them together? A SearchDaemon, for instance?
  
end