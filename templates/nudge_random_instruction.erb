class <%=@instname%> < Instruction
  def preconditions?
    true # no preconditions
  end
  def setup
  end
  def derive
    @result = ValuePoint.new("<%=@type%>", <%=@camelized_type%>.any_value)
  end
  def cleanup
    pushes :<%=@type%>, @result
  end
end
